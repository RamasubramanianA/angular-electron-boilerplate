/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { DynCompDirHostDirective } from './DynCompDirHost.directive';

describe('Directive: DynCompDirHost', () => {
  it('should create an instance', () => {
    const directive = new DynCompDirHostDirective();
    expect(directive).toBeTruthy();
  });
});
